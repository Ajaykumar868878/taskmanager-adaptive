services:
  - type: web
    name: smart-task-manager
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
    databases:
      - name: smart-task-db
        plan: free
